import React from 'react';
import Direction from './Direction';

const GoogleMap = () => {
    return (
        <div>
            <Direction></Direction>
        </div>
    );
};

export default GoogleMap;